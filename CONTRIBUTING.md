niente delay();
niente while() bloccanti;
usare millis();
snprintf() con buffer dimensionati;
niente String nelle nuove funzionalità;
WebServer indipendente dalla logica di controllo;
errori I²C non devono fermare il firmware;
ogni patch deve compilare;
ogni patch deve aggiornare CHANGELOG.md;
ogni patch deve essere coerente con ROADMAP.md.